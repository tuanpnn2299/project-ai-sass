/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "oaidalleapiprodscus.blob.core.windows.net.images.remotePatterns",
    ],
  },
};

export default nextConfig;
